# Notebook Parity Notes

The original project logic was notebook-centric. The new CLI scripts cover the same lifecycle:

- Build splits
- Train models
- Evaluate checkpoints
- Run inference
- Visualize sample overlays

Keep notebooks for exploration, but use CLI entry points for reproducible experiments and CI-friendly execution.
